# NetTool.bat
A multi-stage Batch-based dropper designed to simulate complex infection chains. This project demonstrates how a seemingly benign network utility (NetTool.bat) can be used to coordinate and execute modular sub-processes (dependencies) within a Windows environment.  2. The "Red Team" Style (Professional)
